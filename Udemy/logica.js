// uma funçao que recebe 2 numeros e retorna o maior deles

function max (x,y){ 
     return x > y ? x : y // (se x for maior que y retorna x senao y e)
    /*
    if (x > y ){
        //return x 
    } 
    return y
    */
}
console.log(max(10,2))

/*
const max2  = (x,y) => {
    return x > y ? x : y 
}
*/
const max2 = (x , y) => x > y ? x : y
console.log(max2(40,25))


    